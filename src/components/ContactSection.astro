<section id="contact" class="contact-section" aria-labelledby="contact-title">
  <div class="contact-topline">
    <p>04 / Contact</p>
    <p>Available for select collaborations</p>
    <span aria-hidden="true">●</span>
  </div>

  <div class="contact-main">
    <h2 id="contact-title">say hello,<br />i dont bite</h2>
    <a class="contact-email" href="mailto:hello@stayobservant.com">
      <span>hello@stayobservant.com</span>
      <span aria-hidden="true">↗</span>
    </a>
  </div>

  <footer class="contact-footer">
    <a href="#work">Selected work ↑</a>
    <p>Stay curious. Stay observant.</p>
  </footer>
</section>

<style>
  .contact-section {
    color: #f4f0e7;
    background: var(--ink);
    scroll-margin-top: 1rem;
  }

  .contact-topline,
  .contact-footer {
    display: grid;
    grid-template-columns: 1fr 1fr auto;
    gap: 1.5rem;
    align-items: center;
    min-height: 3.8rem;
    padding: 0.75rem var(--page-gutter);
    font-size: 0.62rem;
    font-weight: 700;
    letter-spacing: 0.1em;
    text-transform: uppercase;
  }

  .contact-topline {
    border-bottom: 1px solid rgba(244, 240, 231, 0.3);
  }

  .contact-topline span {
    color: var(--signal);
    font-size: 0.7rem;
  }

  .contact-main {
    padding: clamp(3.5rem, 8vw, 8rem) var(--page-gutter);
  }

  h2 {
    margin: 0;
    font-family: var(--font-display);
    font-size: clamp(3.25rem, 7.4vw, 7.8rem);
    font-weight: 700;
    line-height: 0.76;
    letter-spacing: -0.085em;
    text-transform: uppercase;
  }

  .contact-email {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1.5rem;
    margin-top: clamp(3rem, 6vw, 6rem);
    padding: 1.1rem 0;
    color: inherit;
    border-top: 1px solid rgba(244, 240, 231, 0.4);
    border-bottom: 1px solid rgba(244, 240, 231, 0.4);
    text-decoration: none;
    font-family: var(--font-display);
    font-size: clamp(1rem, 2.5vw, 2.6rem);
    font-weight: 600;
    letter-spacing: -0.04em;
    transition:
      color 320ms cubic-bezier(0.22, 1, 0.36, 1),
      background-color 380ms cubic-bezier(0.22, 1, 0.36, 1),
      padding 380ms cubic-bezier(0.22, 1, 0.36, 1);
  }

  .contact-email:hover,
  .contact-email:focus-visible {
    padding-inline: 1rem;
    color: var(--ink);
    background: var(--signal);
    outline: none;
  }

  .contact-footer {
    min-height: 4.5rem;
    color: rgba(244, 240, 231, 0.58);
    border-top: 1px solid rgba(244, 240, 231, 0.3);
  }

  .contact-footer a {
    color: inherit;
    text-decoration: none;
  }

  @media (max-width: 760px) {
    .contact-section {
      scroll-margin-top: var(--mobile-header-height);
    }

    .contact-topline,
    .contact-footer {
      grid-template-columns: 1fr auto;
    }

    .contact-topline p:nth-child(2),
    .contact-footer p:nth-child(2) {
      display: none;
    }
  }
</style>
